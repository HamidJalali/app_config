apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: demo
type: NodePort
spec:
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
